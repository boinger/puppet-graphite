# !!! generated by puppet !!!

# Schema definitions for whisper files. Entries are scanned in order,
# and first match wins.
#
#  [name]
#  pattern = regex
#  retentions = timePerPoint:timeToStore, timePerPoint:timeToStore, ...

#[carbon]
#pattern = ^carbon\.
#retentions = 60s:90d

[watchertxntimer]
pattern = ^stats\.timers\.service\.watcher\.transaction\..*
retentions = 5m:7d,1d:4w,1w:730d

[default]
pattern = .*
retentions = 10s:24h,60s:2d,300s:730d
#retentions = 10s:1h
